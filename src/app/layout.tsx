import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Pixel Pioneers Lab | Innovative Web Development",
  description: "Transform your ideas into cutting-edge digital experiences. Modern web development, design, and consulting services.",
  keywords: ["web development", "design", "digital agency", "react", "next.js"],
  authors: [{ name: "Pixel Pioneers Lab" }],
  openGraph: {
    title: "Pixel Pioneers Lab",
    description: "Innovative Web Development & Design",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
